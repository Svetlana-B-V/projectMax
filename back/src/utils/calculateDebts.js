import { Expense, ExpenseSplit } from '../models/index.js';

export const calculateGroupDebts = async (groupId) => {
  // Этот метод больше не используется, так как долги хранятся в таблице Debt.
  // Оставлен на случай отладки.
  return [];
};